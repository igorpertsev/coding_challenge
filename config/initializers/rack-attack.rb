class Rack::Attack
  # throttle('req/ip', limit: 100, period: 1.minute) do |req|
  #   req.ip
  # end

  # throttle('import/ip', limit: 1, period: 1.minute) do |req|
  #   req.ip if req.path == '/v1/import' && req.post?
  # end
end
